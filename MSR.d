// 'Encryption' Program.
// Written in D
/* MSR(Month shift right) Fun Program for
ciphering.*/
Month[string] = {"January", "Febuary",
"March", "April", "May", "June", "July",
"August", "September", "October",
"November", "December"};
//Month List
EndLetters[char] = {'y', 'h', 'l', 'e',
't', 'r'};
//Letter List
writeln("Enter a Text: ");
//Enter text
writeln.log = "Input";
//created "Input" assigned to be var for
inputs.

if(check.EndLetters...%){
wrintln();
/*if it notices any of the endletters then
it will just have it stay there and not
change*/
}else{
input...%(Month)){
check.Input(0);
//Goes 1 to the right
  writeln(Input);
//Will be updated later.
